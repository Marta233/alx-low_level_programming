#include "main.h"
#include <stdio.h>

/*
 * main - check the code.
 *
 * Return: Always 0.
 */

void positive_or_negative(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else 
	{
		return (0);
	}
}
