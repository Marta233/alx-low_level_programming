#include <stflib.h>
#include <dog.h>
void init_dog(struct dog *d, char *name, float age, char *owner)
{
if (d == NULL)
	return (NULL);
	d->name = name;
        user->age = age;
        user->owner = owner;
    return user;

}
