#include <stdio.h>

/**
 * main - entry point, prints values of different data types
 *
 * Return: Always return 0
 */
int main(void)
{
	char n;

	for (n = 0 ; n < 10 ; n++)
		printf("%d is the number\n", n);
	return (0);
}
